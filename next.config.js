/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    unoptimized: true,
    // domains: ['instagram.fhkg3-1.fna.fbcdn.net']
  }
}

module.exports = nextConfig
